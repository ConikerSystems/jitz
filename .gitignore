# Jitz — back up code + content only. The move list (moves.json) is public
# reference content and IS committed; never commit private data or secrets.
venv/
__pycache__/
*.pyc
*.pyo
.env
*.log
.DS_Store
.claude/*
!.claude/source.json
# Data / databases (none today, but guard anyway)
*.db
*.sqlite
*.sqlite3
data/

# --- Sensitive files: never commit (Claude Hub standard, added 2026-09-15) ---
*.db
*.sqlite*
*-wal
*-shm
.env
.env.*
!.env.example
*.pem
*.key
*.p12
token*.json
credentials*.json
client_secret*.json
*.sql.gz
*.db.gz
node_modules/
venv/
__pycache__/
.DS_Store
